# nav-rs

A Rust workspace for navigation and estimation math. The `nav-attitude` crate provides rigid-body attitude representations — quaternions, direction cosine matrices, Euler angles, and rotation vectors — with explicit, documented conventions (Hamilton, scalar-first, active rotations).
